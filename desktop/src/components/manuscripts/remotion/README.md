# `src/components/manuscripts/remotion/`

这里是稿件编辑器和 Remotion renderer 共用的 motion composition 子域。

## Current Files

- `VideoMotionComposition.tsx`
- `types.ts`

## Rules

- 编辑器预览协议必须与 `src/remotion/` 和 `remotion/render.mjs` 保持一致。
- 预览 UI 和 composition 类型变更要一起验证。
